#
# Check whether SYSLOG server is configured on devices
#
---
- name: "Check: {{syslog_server}} is not configured as syslog server"
  ios_command:
    commands: "show run | include logging host"
  register: result
  failed_when: "not('logging host ' in result.stdout[0])"
  ignore_errors: yes
